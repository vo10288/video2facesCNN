# Video2Faces - Requirements
# Python 3.8+

# Core dependencies
face_recognition>=1.3.0
dlib>=19.24.0
opencv-python>=4.8.0
Pillow>=10.0.0

# Optional: GPU support (uncomment if using NVIDIA GPU)
# opencv-python-headless>=4.8.0  # Use instead of opencv-python for servers
